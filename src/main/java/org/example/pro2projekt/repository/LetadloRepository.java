package org.example.pro2projekt.repository;

import org.example.pro2projekt.objects.Letadlo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LetadloRepository extends JpaRepository<Letadlo, Integer> {
}
